export enum eGender {
	MALE = "Male",
	FEMALE = "Female",
	OTHER = "Other",
}


export enum eMaritalStatus {
	SINGLE = 'Single',
	MARRIED = 'Married',
	DIVORCED = 'Divorced',
	WIDOWED = 'Widowed',
	SEPARATED = 'Separated'
}

export enum eArticleType ={
	
}